# Default install: core deps + the GPU (CUDA) build of torch/torchvision.
#   pip install -r requirements.txt
#
# If the cu126 wheels below aren't available for your platform (no matching
# NVIDIA/CUDA build, e.g. macOS, or pip reports "No matching distribution"),
# this file will fail outright -- pip has no built-in "try GPU, fall back to
# CPU" logic. Either:
#   pip install -r requirements-cpu.txt      # install the CPU build directly, or
#   python install.py                        # try GPU, auto-fall back to CPU on failure

-r requirements-base.txt

# CNN/ViT models (trainbase/torch_models.py) and the pretrained ResNet18/ViT-B/16
# backbones (trainbase/torch_pretrained_models.py). cu126 = CUDA 12.6 toolkit
# (works with any driver supporting <= CUDA 13.2); every other package above
# still resolves from PyPI.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.13.0+cu126
torchvision==0.28.0+cu126

# --- Optional extras (uncomment as needed; not required for the core pipeline) ---

# VGG16 feature embeddings (preprocessing/vectorize.py), for the embedding_pca /
# embedding_jl pipelines only. Keras runs on the torch backend above (no
# tensorflow needed): pip install keras
# keras

# Dataset download (download_dataset.py); needs Kaggle credentials configured
# for kagglehub: pip install kagglehub
# kagglehub

# Running the test suite: pip install pytest
# pytest
